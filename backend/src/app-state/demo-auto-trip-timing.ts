/** Keep in sync with `src/mobile/constants/demoTripAuto.ts` (Nest cannot import Vite modules). */
export const DEMO_AUTO_TRIP_PROGRESS_DELAYS_MS = {
  toDriverArrived: 3500,
  toInProgress: 4000,
  toCompleted: 4500,
} as const;
