/** Must match mobile `VehicleType` / `SelectedVehicleId`. */
export enum VehicleType {
  Economy = 'economy',
  Basic = 'basic',
  Classic = 'classic',
  Electric = 'electric',
  Minivan = 'minivan',
  Executive = 'executive',
  Hourly = 'hourly',
}
