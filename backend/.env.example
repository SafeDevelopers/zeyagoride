# Nest listen port (also used by root `server.ts` proxy if you align env).
PORT=3000

# PostgreSQL — required for Prisma (`AppStateService` persistence).
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/zeyago?schema=public"

# Demo-only auto trip phase transitions (optional).
DEMO_AUTO_TRIP_PROGRESS=false
